import {type LayerGroup} from 'leaflet';

export interface LayerGroups {
    all: LayerGroup;
    available: LayerGroup;
    notAvailable: LayerGroup;
    capacity1: LayerGroup;
    capacity2: LayerGroup;
    capacity3: LayerGroup;
    capacity4: LayerGroup;
}
